# Generated by `springdocker setup --ci`. Edit freely; regenerate with --force.
name: Dockerfile SSOT

on:
  pull_request:
  push:

jobs:
  dockerfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: {{ action_ref }}
        with:
          project-root: {{ project_root }}
          dockerfile: {{ dockerfile }}
{%- if build_tool %}
          build-tool: {{ build_tool }}
{%- endif %}
{%- if springdocker_version %}
          springdocker-version: "{{ springdocker_version }}"
{%- endif %}
